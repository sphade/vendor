import React from "react";

const Info = () => {
  return (
    <div className="p-5 bg-blue-50  text-gray-700 rounded-lg w-[650px] ">
      Create booking for charter and Jet pooling, so customers can see when and
      where your aircrafts are available. You can also view Order history and
      Upcoming/pending bookings.
    </div>
  );
};

export default Info;
